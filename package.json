{
  "name": "oddsare",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "jest",
    "watch": "yarn run build -w",
    "start:android": "yarn run build && concurrently -r 'npm run watch' 'react-native run-android'",
    "start:ios": "yarn run build && concurrently -r 'yarn run watch' 'react-native run-ios'"
  },
  "dependencies": {
    "react": "16.2.0",
    "react-native": "0.53.0"
  },
  "devDependencies": {
    "babel-jest": "22.2.2",
    "babel-preset-react-native": "4.0.0",
    "jest": "22.3.0",
    "react-test-renderer": "16.2.0"
  },
  "jest": {
    "preset": "react-native"
  }
}